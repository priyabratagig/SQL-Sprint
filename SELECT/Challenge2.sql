-- Challenge:
-- Use a SELECT statement to get the last_name and city of all customers. Alias the last_name to customer_last_name in the query

SELECT last_name as customer_last_name, city
from hcm.employees;